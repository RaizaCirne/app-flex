document.querySelector('.titulo').textContent = "Comprar macã" 
//  Utilizamos querySelector para percorrer a árvore do DOM
// Através da propriedade textContent conseguimos acesso ao texto do parágrafo, e por fim, trocamos o texto.
// Alteramos de forma dinâmica o conteúdo da aplicação. Para isso, percorremos o DOM. 

